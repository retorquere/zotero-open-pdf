{
  "name": "zotero-open-pdf",
  "type": "module",
  "version": "1.0.11",
  "description": "Open PDF",
  "scripts": {
    "lint": "dprint fmt bootstrap.ts content/*.ts && dprint check bootstrap.ts content/*.ts",
    "prebuild": "rm -rf build && mkdir build && npm run lint",
    "build": "tsc --noEmit && node esbuild.js",
    "postbuild": "mkdir -p build/icons && cp icons/* build/icons && zp-zipup build zotero-open-pdf",
    "release": "zp-release",
    "postversion": "git add package.json package-lock.json && git commit --allow-empty -m version && git push --follow-tags",
    "postinstall": "patch-package",
    "prestart": "npm run build",
    "start": "zotero-start",
    "prebeta": "npm run build",
    "beta": "zotero-start --beta"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/retorquere/zotero-open-pdf.git"
  },
  "author": {
    "name": "Emiliano Heyns",
    "email": "emiliano.heyns@iris-advies.com"
  },
  "bugs": {
    "url": "https://github.com/retorquere/zotero-open-pdf/issues"
  },
  "homepage": "https://github.com/retorquere/zotero-open-pdf",
  "dependencies": {
    "dprint": "^0.51.1",
    "esbuild": "^0.27.2",
    "mkdirp": "^3.0.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.1.2",
    "shell-quote": "^1.8.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "zotero-plugin": "^8.0.7",
    "zotero-plugin-toolkit": "^5.1.0-beta.13",
    "zotero-types": "^4.0.5"
  },
  "xpi": {
    "name": "Open PDF for Zotero",
    "updateLink": "https://github.com/retorquere/zotero-open-pdf/releases/download/v{version}/zotero-open-pdf-{version}.xpi",
    "releaseURL": "https://github.com/retorquere/zotero-open-pdf/releases/download/release/",
    "bootstrapped": true,
    "minVersion": "7.0"
  },
  "devDependencies": {
    "jsdom": "^27.4.0",
    "patch-package": "^8.0.1",
    "typescript-eslint": "^8.53.1"
  }
}
